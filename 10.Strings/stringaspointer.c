#include <stdio.h>
#include <string.h>
int main()
{
    // char str[] = {"PhysicsWallah"};
    // char *ptr = str; // ptr now points to str[0]
    // int i = 0;
    // while (*ptr != '\0')
    // {
    //     printf("%c", *ptr);
    //     ptr++;
    //     i++;
    // }

    //   but but
    char *ptr = {"College Wallah"};
    printf("%s", ptr);

    return 0;
}
